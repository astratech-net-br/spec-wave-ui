{
  "name": "infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "IaC (AWS CDK) do SaaS spec-flow-ui: DynamoDB + Cognito + Secrets (StatefulStack), Lambdas + API Gateway (ApiStack), S3 + CloudFront (WebStack). Conta 458889634344.",
  "scripts": {
    "cdk": "cdk",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy --all",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "aws-cdk": "^2.170.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
